version: "2.1"
services:
  kms:
    image: mikolatero/vlmcsd:latest
    container_name: kms
    environment:
      - PUID=1001
      - PGID=1001
      - TZ=Australia/Perth
    volumes:
      - ./config:/config
    ports:
      - 1688:1688
    restart: unless-stopped
